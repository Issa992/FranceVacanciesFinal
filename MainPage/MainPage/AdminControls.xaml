<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainPage.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ViewModel="using:MainPage.ViewModel"
  x:Class="MainPage.AdminControls"
    mc:Ignorable="d">




    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <ContinuumNavigationTransitionInfo/>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid>
        <Grid.DataContext>
            <local:ViewModel/>
        </Grid.DataContext>
        <Grid.Background>
            <ImageBrush ImageSource="Photos/ApartamentPhotos/login.jpg"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{Binding AmsterdamAppList}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}"></TextBlock>
                        <TextBlock Text="{Binding PricePerNight}"></TextBlock>
                        <TextBlock Text="{Binding Description}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <Button Content="Paris Appartments" Grid.Column="1" HorizontalAlignment="Left" Margin="34,28.5,0,0" Grid.Row="1" VerticalAlignment="Top" Height="78" Width="227" FontSize="18" FontWeight="Bold"/>
        <Button Content="Main Page" Grid.Column="1" HorizontalAlignment="Left" Margin="339,28.5,0,0" VerticalAlignment="Top" Width="227" Grid.Row="1" Height="78" FontSize="18" FontWeight="Bold"/>
        <Button Content="Go To customer list" HorizontalAlignment="Left" Margin="198,28.5,0,0" Grid.Row="1" VerticalAlignment="Top" Height="78" Width="227" FontSize="18" FontWeight="Bold"/>

    </Grid>







</Page>
